import { CollectionEditGuard } from './occurrence-edit.guard';

describe('OccurrenceEditGuard', () => {
  it('should be defined', () => {
    expect(new CollectionEditGuard()).toBeDefined();
  });
});
