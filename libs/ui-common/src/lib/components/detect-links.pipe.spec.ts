import { DetectLinksPipe } from './detect-links.pipe';

describe('DetectLinksPipe', () => {
  it('create an instance', () => {
    const pipe = new DetectLinksPipe();
    expect(pipe).toBeTruthy();
  });
});
